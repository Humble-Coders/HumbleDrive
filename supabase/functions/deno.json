{
  "//": "Config for the Edge Functions. `deno task test` is the fast loop: there is no local Supabase stack, so `functions serve` is unavailable and deploying is the final check, not the inner one.",
  "tasks": {
    "test": "deno test --allow-env",
    "test:offline": "deno test --allow-env --deny-net --cached-only",
    "check": "deno check _shared/*.ts admin-me/*.ts drivers/*.ts places-autocomplete/*.ts routes-preview/*.ts trips/*.ts",
    "lint": "deno lint",
    "fmt": "deno fmt"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@^1.0.8"
  },
  "fmt": {
    "lineWidth": 100
  },
  "lint": {
    "rules": {
      "tags": ["recommended"]
    }
  }
}
